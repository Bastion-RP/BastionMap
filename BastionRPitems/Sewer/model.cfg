class CfgSkeletons
{
  class Default{};
  class BastionRP_sewer_cap_door_left: Default
  {
    skeletonInherit = "Default";
    skeletonBones[] =
    {
      "Doors1",""
    };
  };
  class BastionRP_sewer_side_door: Default
  {
    skeletonInherit = "Default";
    skeletonBones[] =
    {
      "Doors1",""
    };
  };
  class BastionRP_sewer_side_door_right: Default
  {
    skeletonInherit = "Default";
    skeletonBones[] =
    {
      "Doors1",""
    };
  };
};
class CfgModels
{
  class Default{};
  class BastionRP_sewer_cap_door_left: Default
  {
    skeletonName = "BastionRP_sewer_cap_door_left";
    sections[] = {};
    class Animations
    {
      class Doors
      {
        type        = rotation;
        source      = Doors1; // Controler defined in class AnimationSources in config.cpp.
        selection   = Doors1; // Selection from p3d that also has to be defined as a bone in this model`s skeleton.
        axis        = Doors1_axis; // Axis of the rotation.
        memory      = 1;
        minValue    = 0; // Value of the controller at which the rotation will be angle0
        maxValue    = 1; // Value of the controller at which the rotation will be angle1
        angle0      = 0;
        angle1      = (rad 98); // "rad" converts from degrees to radians
      };
    };
  };
  class BastionRP_sewer_side_door: Default
  {
    skeletonName = "BastionRP_sewer_side_door";
    sections[] = {};
    class Animations
    {
      class Doors
      {
        type        = rotation;
        source      = Doors1; // Controler defined in class AnimationSources in config.cpp.
        selection   = Doors1; // Selection from p3d that also has to be defined as a bone in this model`s skeleton.
        axis        = Doors1_axis; // Axis of the rotation.
        memory      = 1;
        minValue    = 0; // Value of the controller at which the rotation will be angle0
        maxValue    = 1; // Value of the controller at which the rotation will be angle1
        angle0      = 0;
        angle1      = (rad 98); // "rad" converts from degrees to radians
      };
    };
  };
  class BastionRP_sewer_side_door_right: Default
  {
    skeletonName = "BastionRP_sewer_side_door_right";
    sections[] = {};
    class Animations
    {
      class Doors
      {
        type        = rotation;
        source      = Doors1; // Controler defined in class AnimationSources in config.cpp.
        selection   = Doors1; // Selection from p3d that also has to be defined as a bone in this model`s skeleton.
        axis        = Doors1_axis; // Axis of the rotation.
        memory      = 1;
        minValue    = 0; // Value of the controller at which the rotation will be angle0
        maxValue    = 1; // Value of the controller at which the rotation will be angle1
        angle0      = 0;
        angle1      = (rad 98); // "rad" converts from degrees to radians
      };
    };
  };
};