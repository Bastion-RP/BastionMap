class CfgSkeletons
{
  class Default
  {
    isDiscrete = 1;
    skeletonInherit = "";
    skeletonBones[] = {};
  };
  class Skeleton_1Door: Default
  {
    skeletonInherit = "Default";
    skeletonBones[] =
    {
      "Doors1",""
    };
  };
};
class CfgModels
{
  class Default
  {
    sectionsInherit = "";
    sections[] = {};
    skeletonName = "";
  };
  class BastionRP_barrier1: Default
  {
    skeletonName = "Skeleton_1Door";
    class Animations
    {
      // Animation for rotating barrier
      class door1
      {
        type        = rotation;
        source      = Doors1; // Controler defined in class AnimationSources in config.cpp.
        selection   = Doors1; // Selection from p3d that also has to be defined as a bone in this model`s skeleton.
        axis        = Doors1_axis; // Axis of the rotation.
        memory      = 1;
        minValue    = 0; // Value of the controller at which the rotation will be angle0
        maxValue    = 0.23; // Value of the controller at which the rotation will be angle1
        angle0      = 0;
        angle1      = (rad -90); // "rad" converts from degrees to radians
      };
    };
  };
};